# 复制为 local.properties，并把路径改成你自己的 Android SDK
sdk.dir=/Users/yourname/Library/Android/sdk

# OAuth App（https://github.com/settings/developers → OAuth Apps → New OAuth App）
# Authorization callback URL 必须填：airepo://oauth2redirect
# 也可以用环境变量 GITHUB_CLIENT_ID / GITHUB_CLIENT_SECRET 注入，避免写进版本库
